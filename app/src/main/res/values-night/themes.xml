<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://android.com/tools">
    <style name="Theme.PlaylistMaker" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Фон главного экрана в темной теме: черный вместо синего -->
        <item name="colorPrimary">@color/background</item>
        <item name="colorPrimaryVariant">@color/background</item>
        <item name="colorOnPrimary">@color/black</item>

        <item name="colorSecondary">@color/accent</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Фон страницы в темной теме -->
        <item name="android:colorBackground">@color/background</item>

        <!-- Фон кнопок: белый (как на фото) -->
        <item name="colorSurface">@color/background</item>

        <!-- Текст и иконки: белый -->
        <item name="colorOnSurface">@color/white</item>

        <item name="colorError">@color/accent</item>
        <item name="colorOnError">@color/white</item>

        <!-- Цвет статус-бара -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/background</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>
</resources>